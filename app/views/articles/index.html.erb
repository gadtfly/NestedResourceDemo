<h1>
  Articles
</h1>

<ul>
  <% @articles.each do |article| %>
    <li>
      <%= link_to article.title, [@wiki, article] %>
    </li>
  <% end %>
</ul>

<%= link_to 'New Article', [:new, @wiki, :article] %>
<%#= link_to 'New Article', new_wiki_article_path(@wiki) %>